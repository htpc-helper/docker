export LC_ALL=C
export DEBIAN_FRONTEND=noninteractive
export USER=htpc-helper
export UID=1000
export TERM=xterm
export LANG=en_AU.UTF-8
export LANGUAGE=en_AU:en
export LC_ALL=en_AU.UTF-8
minimal_apt_get_install='apt-get install -qy --no-install-recommends'

# Default services
# Set 1 to the service you want to disable
export DISABLE_SYSLOG=${DISABLE_SYSLOG:1}
export DISABLE_SSH=${DISABLE_SSH:1}
export DISABLE_CRON=${DISABLE_CRON:1}
